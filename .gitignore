# Local .terraform directories
**/.terraform/*

# .tfstate files
*.tfstate
*.tfstate.*

# Ignore transient lock info files created by terraform apply
.terraform.tfstate.lock.info

# Ignore Terraform lock file (not good practice, but want repo to be clean).
.terraform.lock.hcl
